- recommed use lowercase
- Using double quotes, or single (".') is unnecessary
- Use HTML headings for headings only. Don't use headings to make text BIG or bold.
- The HTML <head> element contains meta data. Meta data are not displayed. <head> element is placed between the <html> tag and the <body> tag.
